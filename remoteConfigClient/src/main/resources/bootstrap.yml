spring:
    cloud:
        config:
            name: configClientA
            label: master
            discovery:
                enabled: true
                service-id: remoteConfigServer
eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/